# 彦博 AI 手机应用安装与更新说明

## 当前版本

模型身份：

```text
彦博-v3
```

Android 安装包：

```text
D:\LLM\releases\Yanbo-AI-Android-v1.1.10.apk
```

Android 应用商店包：

```text
D:\LLM\releases\Yanbo-AI-Android-v1.1.10.aab
```

iOS 原生工程包：

```text
D:\LLM\releases\Yanbo-AI-iOS-Project-v1.1.10.zip
```

完整手机发布包：

```text
D:\LLM\releases\Yanbo-Mobile-Release-v1.1.10.zip
```

## Android 安装

把 `Yanbo-AI-Android-v1.1.10.apk` 传到手机并安装。该版本继续使用固定发布签名，可以直接覆盖旧版本。

应用已经内置公网服务器地址和访问配置，不需要填写服务器地址，也不要求手机与电脑处于同一 Wi-Fi。打开后即可使用彦博-快速、彦博-思考和彦博-专家三种模式。

三种模式都由同一个彦博-v3提供能力，只是回答速度、上下文和输出预算不同。

## 电脑端自动运行

彦博公网服务可注册为 Windows 登录后后台自启动。电脑需要保持开机、联网，并保持公网转发服务正常。

标准公网地址：

```text
https://laptop-m4o3b2hb.tail692923.ts.net/yanbo
```

重新注册自启动：

```text
D:\LLM\11_enable_auto_start.bat
```

关闭自启动：

```text
D:\LLM\12_disable_auto_start.bat
```

手动检查或修复公网服务：

```text
D:\LLM\07_secure_mobile_access.bat
```

## 手机端能力

- 新建、切换、搜索、重命名、清空和删除对话；
- 彦博-快速、彦博-思考、彦博-专家三种模式；
- 文字流式聊天；
- 拍照和相册图片上传；
- OCR 文字识别与图片做题；
- Markdown 和代码块显示；
- 复制与重新生成；
- 网络断开自动重连；
- 回到后台后电脑端继续生成；
- 应用重新打开后自动接回未完成任务；
- 请求去重，避免重复生成；
- 应用内检查更新、显示下载进度并打开安装界面。

## 发布后续更新

快速发布下一个 Android 版本：

```text
D:\LLM\09_publish_mobile_update.bat
```

重新构建当前版本 APK 和 AAB：

```text
D:\LLM\08_build_android_app.bat
```

当前 1.1.10 正式 APK 已完成签名验证，更新清单保存了文件大小和 SHA-256，用于旧版应用检查更新。

## iPhone 和 iPad

有两种使用方式：

1. 使用 Safari 打开公网 HTTPS 地址，并添加到主屏幕；
2. 解压 `Yanbo-AI-iOS-Project-v1.1.10.zip`，在 macOS/Xcode 中选择自己的开发团队后签名安装。

Windows 可以生成 iOS 工程包，但最终签名和 IPA 发布必须在 macOS/Xcode 中完成。

## 必须备份

```text
D:\LLM\mobile_app\android\signing
D:\LLM\remote_access.json
```

不要公开发布签名文件、访问令牌或包含私人访问配置的安装包。

## 故障检查

手机无法连接时依次检查：

1. 电脑是否开机并联网；
2. 本地模型列表中是否存在 `yanbo-v3:latest`；
3. 本地 7860 端口是否正在监听；
4. `http://127.0.0.1:7860/api/status` 是否返回成功；
5. 公网地址的 `/api/status` 是否可访问；
6. `.runtime\remote_service.log` 是否记录启动错误。
