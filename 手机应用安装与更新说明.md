# 彦博 AI 手机应用安装与更新说明

## 当前版本

Android 安装包：

```text
D:\LLM\releases\Yanbo-AI-Android-v1.1.3.apk
```

Android 应用商店包：

```text
D:\LLM\releases\Yanbo-AI-Android-v1.1.3.aab
```

iOS 工程包：

```text
D:\LLM\releases\Yanbo-AI-iOS-Project-v1.1.3.zip
```

完整发布包：

```text
D:\LLM\releases\Yanbo-Mobile-Release-v1.1.3.zip
```

## Android 安装

将 `Yanbo-AI-Android-v1.1.3.apk` 传到手机并安装。该版本继续使用原来的固定签名，可以直接覆盖安装旧版。安装 1.1.3 后，后续版本会在应用内弹出更新窗口，点击“立即更新”即可下载并打开安卓安装确认界面。

应用已经内置公网服务器地址和访问凭据，不需要填写服务器地址，不需要安装 Tailscale，也不要求手机与电脑处于同一 Wi-Fi。

打开应用后会直接显示：

```text
你好，我是彦博AI
```

随后即可输入问题、拍照或选择图片。

## 电脑端自动运行

彦博公网服务已经注册到当前 Windows 用户的启动目录。电脑登录 Windows 后会自动在后台启动，不弹出窗口，不需要每次点击 `07_secure_mobile_access.bat`。

后台守护程序每 30 秒检查一次本地服务，异常退出后会自动恢复。公网地址为：

```text
https://laptop-m4o3b2hb.tail692923.ts.net:8443/
```

电脑仍需保持开机、联网，并保持 Tailscale 正常登录。

重新注册自启动时运行：

```text
D:\LLM\11_enable_auto_start.bat
```

关闭自启动时运行：

```text
D:\LLM\12_disable_auto_start.bat
```

`07_secure_mobile_access.bat` 现在只用于手动检查或修复公网服务，日常不需要点击。

## 应用界面

Android 端已做以下精简：

- 删除设置按钮；
- 删除服务器地址和访问令牌输入；
- 删除连接状态、模型版本等技术信息；
- 开场话只保留“你好，我是彦博AI”；
- 支持新建、切换、搜索、重命名和删除独立对话；
- 保留文字聊天、拍照、相册图片和自动更新；
- 支持“彦博-思考”和“彦博-快速”两种回答模式，并按对话保存选择；
- 加入流式心跳、请求去重、回答中断恢复和 Android 网络切换监听；
- 发现新版本时弹出更新说明，应用内显示下载进度并自动打开安装界面；
- 启动时自动预连接服务器；
- 新图标使用蓝紫背景，不再显示白色方框。

## 发布后续更新

运行：

```text
D:\LLM\09_publish_mobile_update.bat
```

发布工具会自动增加版本号、同步手机工程、生成图标、注入固定服务器配置、构建签名 APK/AAB，并更新下载信息。

## 必须备份

```text
D:\LLM\mobile_app\android\signing
D:\LLM\remote_access.json
```

不要公开发布签名文件、访问令牌或包含个人访问凭据的正式 APK。
