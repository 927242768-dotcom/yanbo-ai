# 彦博-v3能力模式说明

## 1. 统一身份

彦博项目只有一个 AI 身份：**彦博-v3**。

`彦博-快速`、`彦博-思考`、`彦博-专家` 不是三个不同模型，而是同一个 `yanbo-v3:latest` 的三种运行参数。任何模式下，AI 的名字、身份、记忆规则和安全规则都保持一致。

## 2. 三种模式

### 彦博-快速

- 运行模型：`yanbo-v3:latest`
- 上下文：8192
- 最大文字输出：4096 tokens
- 特点：优先直接、简洁、快速
- 适合：普通问答、概念解释、短代码和日常聊天

### 彦博-思考

- 运行模型：`yanbo-v3:latest`
- 上下文：12288
- 最大文字输出：8192 tokens
- 特点：更完整地分析条件、步骤和可能错误
- 适合：课程题、代码检查、项目分析和多步骤任务

### 彦博-专家

- 运行模型：`yanbo-v3:latest`
- 上下文：16384
- 最大文字输出：12288 tokens
- 特点：使用更大的上下文和输出预算，强调核验、边界和完整性
- 适合：长代码、复杂推理、专业资料和大型项目分析

三种模式只允许修改上下文、输出预算、温度等生成参数，不能改成其他模型名称。

## 3. 配置文件

模式配置位于：

```text
capability_config.json
```

统一要求：

```json
{
  "fast": {
    "display_name": "彦博-快速",
    "backend": "native",
    "model": "yanbo-v3:latest"
  },
  "thinking": {
    "display_name": "彦博-思考",
    "backend": "native",
    "model": "yanbo-v3:latest"
  },
  "expert": {
    "display_name": "彦博-专家",
    "backend": "native",
    "model": "yanbo-v3:latest"
  }
}
```

`capability_config.py` 会强制锁定三种模式的模型字段，即使配置文件被误改，也会恢复为彦博-v3。

## 4. 本地运行模型

本机保留两个彦博专用别名：

```text
yanbo-v3-core:latest
yanbo-v3:latest
```

其中：

- `yanbo-v3-core:latest` 是本机核心运行别名；
- `yanbo-v3:latest` 是快速、思考和专家模式统一调用的正式运行模型；
- 对外界面、回答、文档和发布信息只展示彦博-v3身份。

重新构建正式运行模型：

```text
python release_model.py
```

## 5. 兼容训练

兼容训练目录已统一为彦博命名：

```text
models\yanbo-v3-compat
adapters\yanbo-v3-compat-lora
```

兼容训练主要用于：

- 固定回答风格；
- 强化格式约束；
- 补充小范围题型；
- 在正式运行服务不可用时提供兼容能力。

日常的快速、思考和专家模式不使用不同身份，始终由彦博-v3提供回答。

当前训练状态：

- 已完成 3 轮；
- 正式适配器累计 700 步；本轮尝试到 750 步，因独立任务得分退化而回滚；
- 第3轮专项验证损失由 `2.3966` 降至 `2.2848`，该口径与旧综合验证损失 `0.8843` 不同；
- 原始兼容模型专项得分由 7/18 提升到 8/18；
- 叠加确定性能力层后，正式引擎专项评测达到 18/18；
- 训练断点保存在 `adapters\yanbo-v3-compat-lora`。

## 6. 本地知识库

资料目录：

```text
knowledge\
```

导入方式：

```text
13_import_knowledge.bat
```

或：

```text
python import_knowledge.py "D:\资料文件夹" "D:\说明书.pdf"
```

知识库会在提问时自动检索相关片段，并交给彦博-v3分析。当前检索适合课程章节、技术参数、型号、代码符号和明确术语。

## 7. 图片能力

当前默认流程为：

```text
图片增强 → OCR → 文字与公式校验 → 彦博-v3分析
```

三种模式都使用同一套图片识别入口，只是分析预算不同。图片存在歧义、缺字或缺少条件时，彦博-v3必须明确指出。

## 8. 身份回归检查

运行：

```text
python verify_identity.py
```

检查内容：

- `model_identity.json` 必须是彦博-v3；
- 快速、思考、专家必须全部使用 `yanbo-v3:latest`；
- Git 公开文本不能出现外部模型品牌；
- 三种模式后端必须锁定为本地彦博-v3。

每次改模型配置、文档或发布流程后，都应先运行该检查再提交 Git。

本次统一升级验证结果：

```text
身份统一检查：通过
能力分层与知识检索：5/5
模型数据联动与指令遵循：10/10
快速、思考、专家真实身份回答：均为彦博-v3
```
