# 彦博能力升级与专家模式说明

## 1. 当前模型分层

彦博现在不再把 0.5B LoRA 兼容模型当作主要能力来源。

- **高性能运行时**：当前 `yanbo-v3:latest` 为约 8B 参数的量化模型，是日常聊天、思考和图片 OCR 后分析的主要后端。
- **兼容回退模型**：`models/Qwen2.5-0.5B-Instruct` 加 LoRA，只在高性能运行时不可用或显式使用 fallback 时加载。
- **专家后端**：可单独配置更强的本地模型，或配置兼容 Chat Completions 的远程模型服务。

LoRA 继续训练仍适合统一表达风格、固定格式和补强小范围任务，但不再被当作突破复杂推理上限的主要方法。

## 2. 三种回答模式

配置文件：

```text
capability_config.json
```

### 彦博-快速

- 使用高性能本地模型
- 较短输出和上下文
- 适合普通问答、简单代码和快速解释

### 彦博-思考

- 使用高性能本地模型
- 更长上下文和输出上限
- 适合课程题目、代码分析和多步骤问题

### 彦博-专家

- 默认仍可使用本地模型，但上下文和输出预算更高
- 可改为更强的本地模型
- 可连接远程专家模型
- 适合复杂推理、长代码、专业资料和高难度图片分析

## 3. 使用更强的本地专家模型

先确保目标模型已经能被本机运行服务识别，再修改 `capability_config.json` 中的 `expert`：

```json
{
  "expert": {
    "backend": "native",
    "model": "你的更强本地模型名称",
    "num_ctx": 16384,
    "text_max_tokens": 3200,
    "image_max_tokens": 3600,
    "direct_vision": false,
    "knowledge_base": true
  }
}
```

也可以临时设置环境变量而不改文件：

```text
YANBO_EXPERT_LOCAL_MODEL=你的更强本地模型名称
```

当前电脑约 20 GB 内存，没有独立 NVIDIA 显卡。更大的量化模型可能可以运行，但速度和内存占用会明显增加，因此应先实测稳定性。

## 4. 连接远程专家模型

在 `capability_config.json` 的 `expert` 中填写：

```json
{
  "expert": {
    "backend": "auto",
    "remote_api_url": "https://你的服务地址",
    "remote_model": "模型名称",
    "direct_vision": true,
    "knowledge_base": true
  }
}
```

访问密钥不要写进项目文件，使用环境变量：

```text
YANBO_EXPERT_API_KEY=你的访问密钥
```

也可以使用：

```text
YANBO_EXPERT_API_URL=https://你的服务地址
YANBO_EXPERT_MODEL=模型名称
```

只有确认远程模型确实支持图片输入时，才把 `direct_vision` 设为 `true`。使用远程后端时，问题内容、相关本地知识片段以及上传的图片可能会发送到该服务，应根据资料隐私决定是否启用。

## 5. 本地专业知识库

资料目录：

```text
knowledge\
```

导入方法：

1. 把 PDF、DOCX、图片、文本、代码文件或整个文件夹拖到 `13_import_knowledge.bat` 上；
2. 工具会把内容转换到 `knowledge\generated\`；
3. 下一次提问时自动检索相关片段；
4. 不需要重新训练，也不需要重启服务。

命令行方式：

```text
python import_knowledge.py "D:\课程资料" "D:\项目文档\说明书.pdf"
```

知识库资料默认不进入 Git，避免把私人文档提交到仓库。

当前检索是轻量本地文本检索，适合型号、术语、课程章节、技术参数和代码符号等明确内容。后续资料规模很大时，可以再升级为向量检索和重排序。

## 6. 图片能力说明

当前本机运行模型虽然声明了视觉能力，但实际接口测试无法稳定读取原图。因此当前默认设置是：

```text
direct_vision=false
```

本机图片流程仍为：

```text
图片增强 → OCR → 文字与公式校验 → 高性能模型分析
```

代码已经支持把原图发送给视觉后端；当专家后端经过真实图片测试后，可将专家模式的 `direct_vision` 改为 `true`。这样高难度图形、连线、表格和位置关系才会由视觉模型直接分析，OCR 只作为辅助。

## 7. 建议使用方式

- 简单问答：彦博-快速
- 普通课程题、代码检查：彦博-思考
- 长代码、复杂推理、专业资料：彦博-专家
- 个人资料问答：先导入知识库，再使用思考或专家模式
- 高难度图片：使用经过验证且开启视觉能力的专家后端

## 8. 回归测试

能力升级专项测试：

```text
python evaluate_capability_upgrade.py
```

完整测试仍可运行：

```text
python evaluate.py
python evaluate_multimodal.py
python evaluate_web_upload.py
python evaluate_mobile_app.py
```
