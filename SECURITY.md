# 安全策略

## 私密配置

本项目的运行密钥和私密资料必须保存在 Git 仓库之外。以下路径已由 `.gitignore` 排除，不得强制加入版本控制：

```text
remote_access.json
mobile_app/android/signing/
models/
adapters/
knowledge/generated/
releases/
.env
.env.*
```

## 凭据泄露处理

发现访问令牌、API 密钥、签名密码或私钥被提交后，应立即：

1. 停止继续推送和发布；
2. 撤销或轮换已经暴露的凭据；
3. 从完整 Git 历史中移除敏感内容，而不是只在最新提交中删除；
4. 重新检查构建产物和移动端资源是否嵌入旧凭据；
5. 确认无泄露后再恢复发布。

仅删除工作区中的文件不能消除已经进入 Git 历史的秘密。

## 远程专家模型

远程专家模式可能把用户问题、本地知识片段和上传图片发送到第三方服务。启用前必须确认：

- 服务地址可信；
- API 密钥通过环境变量提供；
- 隐私资料允许发送到该服务；
- 视觉接口已经过真实图片验证。

## 安全问题处理

本仓库计划作为私有仓库维护。安全问题应通过仓库所有者的私下渠道处理，不要在公开 Issue 中粘贴访问令牌、密码、私钥、签名文件或用户资料。
